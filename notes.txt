# Go to folder 

$ /usr/local/bin/python3 -m venv .venv
$ source .venv/bin/activate
$ python3 -m pip install -r requirements.txt

# Create an environment variable which point to key
export AAA_API_KEY="XXX"